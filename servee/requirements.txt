## Django>=1.3
-e git+http://github.com/django/django.git#egg=Django
BeautifulSoup>=3.2
django-improved-inlines>=0.2.1

PIL
easy-thumbnails
django-classy-tags

# It's presumed that django-uni-form will
# Very soon have some support for fieldsets.
# That's great. I assume the 0.8.0 release
django-uni-form

# I wish there was a pip file called "optionals".
# South isn't actually required, except
# For development of contrib apps.
South>=0.7
